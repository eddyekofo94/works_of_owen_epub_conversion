# CSS Audit: volume_1.epub

- Status: **WARN**
- Errors: 0
- Warnings: 24

## Summary

- CSS files: 1
- CSS classes defined: 48
- XHTML classes used: 24
- XHTML files scanned: 84
- @font-face rules: 8
- Embedded fonts: 8

## Warnings

- `orphan_class`: Class '.ContentsDescWrap' defined in CSS but never used in XHTML
- `orphan_class`: Class '.author' defined in CSS but never used in XHTML
- `orphan_class`: Class '.by' defined in CSS but never used in XHTML
- `orphan_class`: Class '.digression-heading' defined in CSS but never used in XHTML
- `orphan_class`: Class '.editor' defined in CSS but never used in XHTML
- `orphan_class`: Class '.footnote-ref' defined in CSS but never used in XHTML
- `orphan_class`: Class '.front-matter-body' defined in CSS but never used in XHTML
- `orphan_class`: Class '.front-matter-title' defined in CSS but never used in XHTML
- `orphan_class`: Class '.g' defined in CSS but never used in XHTML
- `orphan_class`: Class '.greek' defined in CSS but never used in XHTML
- `orphan_class`: Class '.hebrew' defined in CSS but never used in XHTML
- `orphan_class`: Class '.md' defined in CSS but never used in XHTML
- `orphan_class`: Class '.noindent' defined in CSS but never used in XHTML
- `orphan_class`: Class '.publisher' defined in CSS but never used in XHTML
- `orphan_class`: Class '.scholastic-anchor' defined in CSS but never used in XHTML
- `orphan_class`: Class '.scholastic-label' defined in CSS but never used in XHTML
- `orphan_class`: Class '.separator' defined in CSS but never used in XHTML
- `orphan_class`: Class '.small-caps' defined in CSS but never used in XHTML
- `orphan_class`: Class '.subtitle' defined in CSS but never used in XHTML
- `orphan_class`: Class '.titlepage' defined in CSS but never used in XHTML
- `orphan_class`: Class '.toc-line' defined in CSS but never used in XHTML
- `orphan_class`: Class '.toc-page' defined in CSS but never used in XHTML
- `orphan_class`: Class '.toc-title' defined in CSS but never used in XHTML
- `orphan_class`: Class '.verse' defined in CSS but never used in XHTML
